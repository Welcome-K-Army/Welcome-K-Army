import 'package:flutter/material.dart';
import 'package:syncfusion_flutter_calendar/calendar.dart';

class CustomCalendar extends SfCalendar {
  CustomCalendar({
    Key key,

  }) : super(key: key);
}
